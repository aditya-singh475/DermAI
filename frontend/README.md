Frontend (Vite + React)

1. Install deps:
   cd frontend
   npm install

2. Run dev server:
   npm run dev

3. The frontend expects the FastAPI backend running on the same host (localhost) and the backend provides a proxy in vite.config.js when running dev.
